<table>
  <th>Name</th>
  <tr>
      <a href="/figures/<%= @figure.id %>"><%= @figure.name %></a>
      <ul>
      Landmarks:
        <% @figure.landmarks.each do |landmark| %>
          <li><%= landmark.name %></li>
        <% end %>
      </ul>
      <ul>
      Titles:
        <% @figure.titles.each do |title| %>
          <li><%= title.name %></li>
        <% end %>
      </ul>
    </tr>
</table>

<br>

<a href="/figures/<%= @figure.id %>/edit" class="button">Edit</a>
<a href="/figures" class="button">Figures</a>
<a href="/" class="button">Home</a>
